<h1>Wellcome to the room:
 <%= @room.name%></h1>

 <h1> messages: </h1>
   <% @room_messages.each do |room_message| %>
   <table>
	   <tr>
	   
	   <%= User.find_by_id(room_message.user_id).name %>  : 
	   <%= room_message.description %>

	   </tr>
   </table>
   <%end%> 
